# Dependencies are installed on the EAS builder.
node_modules/

# Local Expo and native build output must not be uploaded.
.expo/
dist/
web-build/
android/.gradle/
android/build/

# Local environment files.
.env
.env.*

# Backend workspace is not part of the mobile build.
health_connect_backend/
